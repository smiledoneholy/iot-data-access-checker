# Public local-development configuration only. Never put secret values here.
APP_ORIGIN=http://localhost:3000

# Server-only secrets belong in Cloudflare Pages settings or .dev.vars locally.
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
